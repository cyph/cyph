/* eslint-disable */

/** @file firebase/app external. */

const app = eval('require')('firebase/app');

export type FirebaseApp = any;

export const getApps: any = app.getApps;

export const initializeApp: any = app.initializeApp;
