/**
 * Responsible for keeping data and UI in sync.
 * @interface
 * @deprecated
 */
export interface IController {
	/**
	 * Synchronise data and UI. (Now a no-op since ng2 upgrade.)
	 */
	update () : void;
}
