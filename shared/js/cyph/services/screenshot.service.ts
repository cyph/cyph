import {Injectable} from '@angular/core';
import {BaseProvider} from '../base-provider';

/**
 * Angular service for screenshot.
 */
@Injectable()
export class ScreenshotService extends BaseProvider {
	constructor () {
		super();
	}
}
