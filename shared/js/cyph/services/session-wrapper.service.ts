import {Injectable} from '@angular/core';
import {BaseProvider} from '../base-provider';

/**
 * Angular service for session wrapper.
 */
@Injectable()
export class SessionWrapperService extends BaseProvider {
	constructor () {
		super();
	}
}
