version: 2
jobs:
  build:
    docker:
      - image: cyph/circleci:latest
    working_directory: /cyph
    steps:
      - checkout
      - run: |
          touch ~/.circleci
          source ~/.bashrc
          /cyph/commands/build.sh
