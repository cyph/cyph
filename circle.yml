machine:
  services:
    - docker

dependencies:
  override:
    - sed -i 's/.*emsdk.*//g' Dockerfile
    - docker build --rm=false -t cyph/cyph .

test:
  override:
    - docker run -v $PWD:/cyph cyph/cyph /cyph/commands/build.sh
