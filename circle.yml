machine:
  services:
    - docker

dependencies:
  override:
    - sed -i 's/.*emsdk.*//g' Dockerfile
    - docker build --rm=false -t cyph/cyph .

test:
  override:
    - docker run -v .:/cyph cyph/cyph 'source ~/.bashrc ; /cyph/commands/build.sh'
