machine:
  node:
    version: 6.9.1

dependencies:
  override:
    - npm -g install @angular/common @angular/compiler @angular/compiler-cli @angular/core tslint typescript
    - npm install @angular/common @angular/compiler @angular/compiler-cli @angular/core tslint typescript
    - gem install sass

test:
  override:
    - ./commands/build.sh
