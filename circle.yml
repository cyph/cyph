machine:
  services:
    - docker

dependencies:
  override:
    - sed -i 's/.*emsdk.*//g' Dockerfile
    - docker build -t cyph/cyph .

test:
  override:
    - docker run cyph/cyph ./commands/build.sh
